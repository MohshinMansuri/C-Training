// To Check Out A Leap Year

#include <stdio.h>
void maim()
{
    int year;
    printf("Enter year : ");
    Scanf("%d", &year);
    if (year % 4 == 0){
        Pintf("It is a leap year");
    }
    else{
        printf("It is not a leap year");
    }
}